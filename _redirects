# Cloudflare Pages redirects + SPA fallback
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# The single catch-all rule below rewrites any 404 under the root to the
# SPA shell so client-side react-router can take over deep links like
# /posts/2016-08-21-xuch5d. The status is 200 (not 301/302) because this
# is a rewrite, not a redirect.
#
# Put any true redirects ABOVE this line, and they will match first.

/*  /index.html  200
